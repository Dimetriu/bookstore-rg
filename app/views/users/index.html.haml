%main.container
  %h1.general-title-margin= t('.title')
  %ul.nav.clearfix.mb-20{role: "tablist"}
    %li.tab-item.active{role: "presentation"}
      %a.tab-link{href: "#address", 'aria-controls': "address", role: "tab", 'data-toggle': "tab"}= t('.address')
    %li.tab-item{role: "presentation"}
      %a.tab-link{href: "#privacy", 'aria-controls': "privacy", role: "tab", 'data-toggle': "tab"}= t('.privacy')
    -# = link_to "Address", new_user_address_url(current_user), id: 'address', class: 'tab-pane fade in active'
  .tab-content
    #address.tab-pane.fade.in.active{role: "tabpanel"}
      .general-main-wrap

        -# = simple_form_for(:billing_addresses, url: user_billing_address_url(current_user), html: { method: :post}) do |f|
        -#   = f.input :first_name
        -#   = f.button :submit
        -# = simple_form_for(:shipping_address, url: user_shipping_address_url(current_user), html: { method: :post}) do |f|
        -#   = f.input :first_name
        -#   = f.button :submit
    #privacy.tab-pane.fade{role: "tabpanel"}
      .row.mb-60
        .col-sm-5
          %p.in-gold-500.font-18.mb-25= t('.email')
          = render 'email_form'
        .col-sm-5.col-sm-offset-1
          %p.in-gold-500.font-18.mb-25= t('.password')
          = render 'password_form'
      .divider-lg-bottom
        .col-sm-12
          %p.in-gold-500.font-18.mb-25= t('.remove_account')
          = render 'remove_account_form'
